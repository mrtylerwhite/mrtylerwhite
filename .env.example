# Kit (ConvertKit) — never commit real values
# KIT_FORM_ID: newsletter form (e.g. 4969496) — /api/subscribe
# KIT_API_KEY: Kit API v4 — /api/subscribe/roi-case-study
# KIT_ROI_CASE_STUDY_FORM_ID: ROI case study chat opt-in form (8010475)
# KIT_BROADCAST_API_KEY: /api/latest-broadcast (read-only)
KIT_FORM_ID=
KIT_API_KEY=
KIT_ROI_CASE_STUDY_FORM_ID=8010475
KIT_BROADCAST_API_KEY=

# Local `vercel dev`: copy into `.env.local` (preferred) or `.env` (both gitignored).
